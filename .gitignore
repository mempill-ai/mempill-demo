# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
# local demo databases
*.db
*.sqlite
*.sqlite3
# Claude Code MCP config (personal paths — keep .example tracked)
.mcp.json
# Environment variable files — keep .example tracked
.env
.env.*
!.env.example
# Claude Code task sentinels, worktree metadata, and task working docs
.claude/
.mempill/
tasks/
# LangGraph Studio runtime cache (created by `langgraph dev`)
.langgraph_api/
